# Nginx

Placeholder for Nginx/static hosting configuration.

TODO:

- static root for `dist`;
- SPA fallback to `index.html`;
- `/api` reverse proxy strategy if needed;
- gzip/brotli settings;
- cache policy for hashed assets;
- no-cache policy for `index.html`;
- cookie and header forwarding notes.
