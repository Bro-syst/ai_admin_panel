export function ClientHomePage() {
  return (
    <main className="client-page min-h-screen">
      <section className="client-hero" aria-labelledby="client-panel-title">
        <p className="client-eyebrow">Client workspace</p>
        <h1 id="client-panel-title">Client Panel</h1>
        <p className="client-copy">
          A dedicated React application for user-facing flows. Product routes and domain modules will be added here as the
          client scope becomes clear.
        </p>
      </section>
    </main>
  )
}
